{
  "name": "raabta-ai-production",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd .. && node -e \"require('fs').cpSync('frontend/dist', 'dist', {recursive: true})\"",
    "dev": "cd frontend && npm run dev"
  }
}
